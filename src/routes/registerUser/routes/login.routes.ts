import { Router } from "express";
const router = Router();

const passport = require("passport")


/*const {get, 
    post,
    update,
    delete} = require('../controllers/login.controllers');*/

router.route("/")
    .get();



export default router;
